import 'package:flutter/material.dart';

class Normalhomapage extends StatelessWidget {
  const Normalhomapage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('normal home page '),
      ),
    );
  }
}
