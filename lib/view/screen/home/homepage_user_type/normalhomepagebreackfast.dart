import 'package:flutter/material.dart';

class Normalhomepagebreackfast extends StatelessWidget {
  const Normalhomepagebreackfast({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('normal home page breack fast '),
      ),
    );
  }
}
